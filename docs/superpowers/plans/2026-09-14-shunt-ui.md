# Shunt UI Implementation Plan

> **For agentic workers:** Execute inline with superpowers:executing-plans. The user selected implementation by this agent.

**Goal:** Implement the approved six-section UI using the current API and handlers.

**Architecture:** Retain the existing controls and API functions; reorganize their DOM into the approved shell. A small UI model computes route shares and section mappings. Static CSS and a DOM adapter implement the approved overview, routing workspace and grouped navigation without a build step.

**Tech Stack:** Plain HTML/CSS/JavaScript, existing Python API. Node's built-in test runner is development-only.

**Spec:** design/BRIEF.md and the approved prototype at /Users/bkaganovich/Documents/Codex/2026-09-14/new-chat/outputs/shunt.html.

## Global Constraints

- Никакой сборки.
- Preserve all current API endpoints and auth/control handlers.
- Preserve both original palettes, system typography and 10px card radius.
- Russian source strings remain translation keys in static/locales/en.json.
- No fabricated live counts, uptime, healthy state or pending-check verdicts.
- Do not deploy to the gateway in this task.

### Task 1: Model and regression coverage

Files: src/web/static/ui-model.js; tests/ui-model.test.cjs.

- [x] Implement pure `ShuntUIModel.sectionFor(page)`, `trafficShares(summary)`, `egress(status)` and `discovery(rows)`.
- [x] Exercise all legacy pages, blocked/other traffic, zero/unavailable counts, FPTN selection and disabled candidates using `node --test tests/ui-model.test.cjs`.

### Task 2: Approved shell and live dashboard

Files: src/web/static/index.html, ui.css, ui.js.

- [x] Replace navigation with six groups. Move old dashboard speed/control blocks to Diagnostics/System, retaining every control id.
- [x] Adapt the approved overview graphic to values from `/api/analytics/summary?hours=24`; reflect current `/api/status` and `/api/discovered` states.
- [x] Use `/api/network/wan-status` for WAN, status gateway_ip for LAN. Do not invent uptime or geosite entry counts.
- [x] Hook status updates and authentication lifecycle explicitly; stop background UI work after logout or hidden page.

### Task 3: Routing and all grouped screens

Files: src/web/static/ui.js, ui.css, index.html.

- [x] Profile selector and explicit Apply use existing profile handlers. Preserve unsaved selection across status refresh.
- [x] Consolidate manual rules into one filtered table, preserving enable/delete/add/save semantics and validation.
- [x] Move discovered controls to their own tab. Preserve probe/toggle/forget actions.
- [x] Group every legacy page; keep deep links and terminal lifecycle. Make all page content accessible, with secondary tabs for grouped controls.

### Task 4: Localization and browser qualification

Files: src/web/static/locales/en.json; tests/test_ui_static.py; docs/UI.md.

- [x] Add English translations for new Russian strings and isolate changing values from translatable text nodes.
- [x] Verify static resources and all existing DOM ids, then run `python3 -m pytest tests/ -q` and Node tests.
- [x] Run an explicitly labelled local API fixture, never the host network services, for browser interaction QA at 1440×900 and 390×844 in both themes and languages.
- [x] Check login/logout, nav/deep links, live counters, unknown/error states, rules persistence, profile draft/apply, mobile menu and console output.
- [x] Document what was verified locally versus what still needs a gateway qualification run.
